# Мишка в Rive: исходник, сборка, эмоции

> ⚠ Проверка файла аниматора в игровой (заказчик 26.09), не финальный мишка.

## Что где

- `assets_src/rive/bear_boy_v2.rev` — исходник редактора Rive от заказчика
  (26.09). Не правится руками: все изменения делает скрипт.
- `tool/rive/build_bear.py` — сборка: `.rev` → RML (Rive CLI) → правки →
  `.riv`. Результат — `assets/rive/bear_boy_v2.riv`, его играет приложение
  (`lib/bear/rive_bear_trial.dart`).
- Правки скрипта: фон артборда прозрачный (был тёмный), добавлены эмоции
  — девять анимаций `emo_*` (раздел «Эмоции»).

## Rive CLI

Версия 1.1.1, бесплатная, собирает без входа в аккаунт. В облачной среде
сайт `releases.rive.app` должен быть в разрешённых адресах (или архив
присылается вручную). Установка:

    curl -sL https://releases.rive.app/cli/v1.1.1/rive-linux-x64.tar.gz -o rive.tar.gz
    sha256sum rive.tar.gz  # 41684e9d99fea98e01c2c155e07ec985130b95b640410dc0fbe4ca30c271a7d5
    apt-get install -y libegl1 libgles2 libgl1
    mkdir -p /opt/rive-cli && tar xzf rive.tar.gz -C /opt/rive-cli
    ln -sf /opt/rive-cli/rive /usr/local/bin/rive

Вход в аккаунт Rive нужен только чтобы выгрузить изменённый `.rev` обратно
в редактор.

## Скелет (пересобран 26.09, `tool/rive/rebuild_rig.py`)

Старые 13 костей удалены (превращены в неподвижные узлы, чтобы не сбить
порядок отрисовки). Новый скелет — 21 кость, координаты холста 1024×1024:

- корпус снизу вверх: `hips` (таз, y≈812) → `belly` → `chest` (линия плеч,
  y≈560) → `neck` → `head` (опора у шеи, y≈530) → `hood1` → `hood2`
  (кончик капюшона, y≈42);
- `breath` — «вдох» в середине кофты: грудь наполняется;
- `face` — лицо внутри капюшона (сдвиг — поворот головы, взгляд вверх);
  глаза и накладки лица едут за ней;
- `hood_sl`, `hood_sr` — боковины капюшона от висков к плечам;
- мимика (дети `face`): `eye_l`, `eye_r` — мех вокруг глаз (прищур),
  `cheek_l`, `cheek_r` — щёки, `mouth` — рот; веса лица — мягкие зоны
  вокруг этих точек;
- уши `ear_l1/l2`, `ear_r1/r2` — по два звена от основания;
- руки `arm_l1/l2`, `arm_r1/r2` — плечо и предплечье до лапы;
- ноги `leg_l`, `leg_r` — отдельно от таза, стопы стоят на полу.

Веса сеток считаются по положению вершины (`weights_for`): лицо целиком за
головой; капюшон — голова → капюшон1 → капюшон2, низ на плечах — за грудью;
кофта — таз/живот/грудь + «вдох» + боковины под мышкой за плечом; рукава,
уши — вдоль своей цепочки; шорты — таз сверху, ноги снизу.

Жёсткие части (глаза и накладки лица, подкладка капюшона, лапы, стопы) едут
за костями через `TransformConstraint` своих контейнеров на якоря
`anchor_*` внутри костей.

Проверки после сборки: поза покоя совпадает со старой до пикселя; в фазах
дыхания нет дыр внутри силуэта.

## Покой `idle_life` (12 с, 4 вдоха)

Вдох-выдох 3,0 с, глубокий: на выдохе мишка приседает (таз ±5 px), на
вдохе грудь заметно наполняется, грудь и шея откидываются, голова
поднимается и медленно покачивается (6 с). С запаздыванием догоняют
кончик капюшона (сильнее всех), боковины капюшона, уши, плечи и лапы.

Уши движутся по диагонали и складываются к основанию (кость уха сжимается
вдоль своей оси), иногда вздрагивают (`EAR_TWITCH`). Основание уха держится
за голову, чтобы не открывать стык конуса капюшона с полями.

Голова: трижды за петлю осматривается (`LOOKS`) — влево, вправо, вверх:
лицо смещается внутри капюшона, голова наклоняется, капюшон следует.
Моргание — из исходника (бусины сплющиваются, проступают веки), сжато под
12 с; вместе с ним мех вокруг глаз прищуривается и щёки чуть поднимаются.

Стыки уха с конусом капюшона (`EAR_HOOD_CONTACTS`, `pin()`): капюшон за
ухом не ходит совсем, а кончик уха у стыка (радиус 4–62 px, плавно)
держится за конус капюшона. История: сначала стык был прибит к голове —
уголки стояли (заказчик: «неподвижны»); потом общий на ~60 px — за ухом
ходил большой кусок капюшона (заказчик: «нужно короткое соединение»).
Проверка — покадровка обоих стыков по 0,1 с: покой, удивление, обида,
улыбка; край капюшона ровный, щелей нет. Подёргивания ушей в покое
смягчены (поворот 0,07, складывание 0,06), иначе кончик вытягивался
«рожком».

## Эмоции (26.09, `EMOTION_ANIMS` в `rebuild_rig.py`)

Заказчик 26.09: «мимика должна отрабатывать себя, как на живом человеке»
— глаза, брови, щёки и рот меняются вместе. Сделано, как в Talking Tom,
Live2D, Spine/Moho: у лица есть набор готовых выражений, смена мгновенная
и спрятана за морганием или рывком головы, а движение между сменами дают
кости. Плавные проявления (от них при жевании просвечивали «полоски
старого рта») убраны.

- **Выражения лица целиком** (`faces.py`). Нарисовал Higgsfield
  (gpt_image_2_5, правка фото лица: 12 выражений × 2 варианта = 24
  картинки ≈ 36 кредитов, утверждено заказчиком 26.09). Лучшие 12 —
  `assets_src/rive/faces/`: улыбка, смех, смех поменьше, удивление, грусть,
  жуёт (рот закрыт / приоткрыт), смакует, зевок, сонный, глаза закрыты,
  обида. Маска — зоны глаз с бровями и рта со щеками, мягкий край; нос
  свой, цвет подогнан к текстуре. Каждое выражение — копия сетки лица с
  теми же весами, но с маленькой картинкой-плиткой (u/v пересчитаны);
  лежит поверх бусин и век, включается ключом «hold».
- **Моргание-переход.** `blink` — только закрытые глаза: на 6 кадров
  поверх всего; под ним лицо сменилось — глаз смены не видит.
- **Рты-ступени** (26.09). Промежуточные рты из Higgsfield (4 × 2
  картинки ≈ 12 кредитов, «да» заказчика): только зона рта, лежат над
  лицом смеха или жевания и сменяются каждые 3 кадра — рот открывается и
  закрывается по ступеням, а не прыгает. Смех: сомкнутая улыбка
  (`laugh_m0`) → чуть приоткрыт (`laugh_m1`) → приоткрыт (`laugh_m2`) →
  широкий (сам `laugh`). Жуёт: сомкнут (сам `chew`) → щёлочка
  (`chew_m1`) → приоткрыт (`chew_m2`) → открыт (`chew_m3` из
  `chew_open`). `laugh2` не используется: он уже соседних ступеней.
  Ступени идут в такт подбородку (`Emo.mouths`).
- **Кости эмоций `e_*`.** У каждой кости родитель нулевой длины. Покой
  двигает кость, эмоция — только `e_*`, и они складываются: во время
  эмоции мишка продолжает дышать, моргать и шевелить ушами. Мимику
  лица теперь рисуют выражения; кости дают голову, тело, уши, капюшон,
  подбородок и щёки в такт.
- **Колени** (26.09). В каждой ноге кроме бедра `leg_l/r` есть голень
  `shin_l/r` (от колена под шортами, y 872, до пятки); ступня держится за
  голень, низ штанины плавно переходит с бедра на голень (y 840–908).
  Мишка стоит к нам лицом, поэтому согнутое колено показано как в 2D-
  мультфильмах: голень поднимается (до 32 px), штанина собирается, носок
  чуть наружу, ступня чуть крупнее — ближе к нам.
- Кости лица (`mouth_l/r`, `chin`, `lid_l/r`, `ulid_l/r`, `brow_l/r`…),
  густая сетка (`mesh_refine.py`) и веки из меха (`lids.py`) остались —
  на них держатся моргание и подбородок.

| Анимация | Длина | Что делает |
|---|---|---|
| `emo_smile` | 2,5 с | моргнул — улыбка всем лицом (щёки подпирают глаза, ротик приоткрыт), голова набок с пружинкой, тело вверх, уши и капюшон догоняют |
| `emo_laugh` | 3,4 с | зажмурился дугами, рот широко — присел и прыгнул от радости (лапы вверх, ножки поджаты, уши и капюшон отстают), мягко приземлился — пять плавных «ха-ха»: рот по ступеням приоткрыт → широкий → приоткрыт, подбородок, щёки, грудь и голова в такт; в конце рот смыкается в улыбку |
| `emo_surprised` | 2,2 с | короткое моргание — глаза круглые, брови вверх, ротик «о», подпрыгнул, уши торчком |
| `emo_sad` | 3 с | вздох, моргнул — брови домиком, глаза прикрыты, уголки вниз; голова, уши, плечи никнут |
| `emo_chew` | 2,8 с | довольный прищур, щёки набиты, жуёт: рот по ступеням сомкнут → щёлочка → приоткрыт → открыт и обратно, подбородок плавно вверх-вниз и вбок, щёки по очереди |
| `emo_lick` | 2,2 с | блаженный прищур, язычок облизывает губу, голова набок |
| `emo_yawn` | 3,5 с | глаза зажмурились, рот широко, голова назад, лапы в стороны; рот закрылся, открыл глаза, встряхнулся |
| `emo_sleepy` | 3,5 с | веки тяжёлые — задремал (глаза закрыты), голова клюёт — вздрогнул, проснулся — снова веки тяжёлые |
| `emo_upset` | 3 с | надулся: веки тяжёлые, губы надуты (без злых бровей); топает левой-правой-левой-правой (колено вверх, вес на другую ногу, удар — тело вздрагивает); «хмф!» — отвернулся и закрыл глаза, уши прижаты |

Касание мишки перебирает все девять по кругу (для проверки), «Игра» —
`laugh`. Для проверки в игровой справа — панель 1–9 с подписями
(`EmotionTestPanel`): номер эмоции удобно называть в правках. Перед
публикацией убрать (`CLAUDE.md`).

| № | Кнопка | Анимация |
|---|---|---|
| 1 | Улыбка | `emo_smile` |
| 2 | Смех | `emo_laugh` |
| 3 | Удивление | `emo_surprised` |
| 4 | Грусть | `emo_sad` |
| 5 | Жуёт | `emo_chew` |
| 6 | Смакует | `emo_lick` |
| 7 | Зевок | `emo_yawn` |
| 8 | Сонный | `emo_sleepy` |
| 9 | Обида | `emo_upset` | Приветственной эмоции при входе в игровую нет.

Проверки: поза покоя до пикселя та же; все эмоции через 0,2 с — без дыр
внутри силуэта; кадры каждой эмоции просмотрены (смена за морганием, швов нет).

## Замечания заказчика 26.09 — сделано

1. **Смех — рот дёргался.** Скачок «широкий ↔ поменьше» убран; рот
   открывается и закрывается по ступеням (раздел «Рты-ступени»), каждые
   3 кадра, в такт подбородку; все кривые костей мягкие.
2. **Жуёт — рывком.** Рот по ступеням сомкнут → щёлочка → приоткрыт →
   открыт и обратно, подбородок плавно вверх-вниз и чуть вбок, щёки по
   очереди.
3. **Обида — глаза злые.** Лицо собрано из готовых частей: тяжёлые веки
   «Сонного» + надутые губы «Обиды» («надулся»); на «хмф!» —
   отвернулся и закрыл глаза. Злое лицо (`upset`) больше не используется.
4. Удивление и грусть — заказчик согласился, что разные.

Проверка плавности: кадры каждые 1/30 с, разница соседних кадров в
области лица. После рта-ступеней: в «ха-ха» разница соседних кадров
8–12 (одна ступень рта), в жевании 3–5; швов по краю зоны рта нет;
оба клипа кончаются точно в покое, дыр нет (кроме старой точки у
подмышки в прыжке — ниже).

Найдено при проверке 26.09 (не трогал, ждёт решения):

- **Проколы у подмышек.** В покое и в эмоциях с движением лап (улыбка,
  грусть, удивление, зевок, «ха-ха») между рукавом и кофтой на
  отдельных кадрах просвечивает фон: точки 3–12 px в кадре 512
  (на телефоне 1–3 px). Было и раньше, прыжок их не добавил.
- Старая проверка «дыр» (`holes.py`) сравнивала с пустым кадром и всегда
  давала ноль; новая (`holes2.py` в песочнице) ищет фон, окружённый
  мишкой.
