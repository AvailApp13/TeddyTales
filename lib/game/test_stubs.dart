/// Заглушки на время испытаний. **Перед публикацией приложения всё это
/// нужно отредактировать** (заказчик 23.09: «поставь себе пометку, что нам
/// нужно будет в дальнейшем это все отредактировать при публикации
/// приложения»). Каждая заглушка выключается в одном месте — здесь.
library;

/// Сколько монет на счету как минимум — при заходе и после каждого ответа
/// сервера.
///
/// Просьба заказчика 20.09: «чтобы всегда на счету при заходе было по 5000
/// монет, для того чтобы можно было тестировать покупку всего». Иначе
/// проверить обстановку целиком нельзя: весь каталог стоит больше трёх
/// тысяч, а зарабатывают монеты уходом, то есть временем.
///
/// Порог держится и после ответа сервера: тот про добавку не знает и
/// присылает свой баланс — так после первого блюда на счету оказывался
/// ноль, и второе блюдо было не купить (заказчик 23.09: «почему я могу
/// купить только одну еду»). Кошелёк по КП 11.1 живёт на сервере и
/// пополняется за уход (КП 6.4); настоящие значения придут оттуда. Чтобы
/// выключить добавку, достаточно поставить здесь ноль.
const int kTestWallet = 5000;

/// На чём закреплён показатель «Еда», пока отрабатываем еду. `null` —
/// показатель живой: растёт от кормления и убывает со временем.
///
/// Заказчик 23.09: «пока на тестах, чтобы отработать всю еду, убираем
/// ограничения, пусть зафиксирую там, к примеру, на 87 %, но есть он может
/// сколько угодно». Сколько раз в день просить кушать и как заполнять
/// шкалу — решение отдельное, до публикации.
// ignore: unnecessary_nullable_for_final_variable_declarations
const double? kTestFood = 87;

/// Показывать ли экран входа при запуске. Заказчик 23.09: «каждый раз
/// нажимаем „Зайти“ — трата времени; не удалять, а с визуала убрать, сразу
/// открывается мишка». Экран цел: выход из аккаунта по-прежнему ведёт
/// на него. Перед публикацией — `true`.
const bool kShowSignIn = false;

/// Множитель замедления всех анимаций для покадровой проверки. Задаётся
/// при сборке: `--dart-define=SLOW_MOTION=8`. В обычной сборке 1.
const double kSlowMotion =
    int.fromEnvironment('SLOW_MOTION', defaultValue: 1) * 1.0;

/// Принудительный покой кухни для съёмки: `--dart-define=KITCHEN_IDLE=sad`
/// (normal / happy / sad / hungry). Пусто — настроение из показателей.
const String kTestKitchenIdle = String.fromEnvironment('KITCHEN_IDLE');

/// Принудительный характер для съёмки реакций на угощение:
/// `--dart-define=KITCHEN_TRAIT=curious`. Пусто — характер мишки.
const String kTestKitchenTrait = String.fromEnvironment('KITCHEN_TRAIT');
