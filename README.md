# TeddyTales — модуль анимированного персонажа

Flutter-модуль мишки на Rive: состояние персонажа, показатели ухода и вся связь
с ригом через State Machine.

Реализовано по **`docs/tz-animator.md`** (контракт с ригом, раздел 8.1) и по
той части **`docs/tz-app.md`**, что описывает персонажа — показатели ухода,
стадии роста, характеры, одежду. Остальное из КП — магазин, мини-игры,
обучение, каталог физических мишек, платежи, панель управления — пока не
трогалось.

## Структура

```
lib/
  main.dart                     дев-панель: все входы State Machine руками
  bear/
    bear.dart                   barrel-экспорт модуля
    bear_rig_spec.dart          контракт с ригом: имена входов + enum'ы значений
    bear_stats.dart             пять показателей ухода + затухание
    bear_mood_policy.dart       свёртка пяти показателей в один вход mood
    bear_state.dart             полное состояние персонажа + слоты одежды
    bear_controller.dart        API для игровой логики (чистый Dart)
    bear_action.dart            действия пользователя + доступность по стадиям
    bear_trait_tracker.dart     формирование характера из действий
    bear_zodiac.dart            12 знаков + таблица склонностей (за Заказчиком)
    bear_initiative.dart        пузырь инициативы
    bear_phrases.dart           30 реплик на трёх языках
    birth_scene_script.dart     субтитры и тайминги сцены рождения
    birth_scene_view.dart       экран сцены рождения
    bear_rig_sink.dart          интерфейс «передать в риг»
    bear_rig_binding.dart       единственный файл, знающий про API Rive
    bear_view.dart              виджет: загрузка .riv, подключение рига
  game/
    game_calendar.dart          игровой возраст: «3 месяца 12 дней»
    pet_profile.dart            карточка питомца: имя, рождение, монеты
    app_section.dart            разделы навигации и замки по стадии
  screens/
    home_screen.dart            главный экран по макету
    dev_screen.dart             пульт: все входы State Machine руками
  widgets/
    pet_header.dart             шапка: имя, возраст, монеты
    care_stats_panel.dart       пять показателей ухода
    pet_speech_bubble.dart      пузырь инициативы и реплики
    app_bottom_nav.dart         нижняя навигация
  theme/
    app_colors.dart             палитра, снятая с макета
    app_theme.dart              тема, радиусы, отступы
docs/
  tz-animator.md                ТЗ для аниматора — контракт с ригом
  tz-app.md                     состав приложения из КП
  rig-naming.md                 чеклист приёмки рига
  runtime-api-note.md           расхождения документов и как они разрешены
  design-review.md              сверка макета с КП: что подтвердилось, что разошлось
  heroes-and-catalog.md         SLOW и JOY, каталог TeddyTales®
  rig-change-request.md         запрос аниматору: раздельные верх и низ
  tz-rive-animation.md          устаревший черновик, оставлен ради MCP-раздела
test/                           тесты логики (без нативного рантайма)
```

### Почему так разложено

`BearController`, `BearState`, `BearCareStats` и `BearMoodPolicy` не импортируют
`package:rive`. Это даёт две вещи: логику можно гонять в тестах без нативных
библиотек `rive_native`, и замена рантайма не задевает игровую логику —
переписывается только `bear_rig_binding.dart` + `bear_view.dart`.

Весь контакт с ригом идёт через интерфейс `BearRigSink` — это граница между
приложением и анимацией.

## Контракт с ригом

Артборд и State Machine — `bear_main`. Приложение управляет персонажем только
через входы (раздел 8.1 ТЗ аниматора): «никаких других способов запуска
анимаций быть не должно».

| Вход | Тип | Откуда берётся значение |
| --- | --- | --- |
| `stage` | 1–5 | `BearState.stage`, меняется только через `growUp()` |
| `mood` | 0–5 | считается из пяти показателей, см. ниже |
| `trait` | 0–5 | `BearState.trait` |
| `variant` | 0–1 | случайно перед `trg_eat` и `trg_pet` |
| `is_walking` | bool | `BearState.isWalking` |
| `skin` | 0–1 | `BearState.skin` |
| `outfit_id` / `headwear_id` / `shoes_id` / `accessory_id` | 0–8 / 0–3 / 0–2 / 0–3 | `BearState.outfit` |
| 11 `trg_*` | Trigger | методы `BearController` |

### Пять показателей → один `mood`

КП задаёт пять показателей 0–100 (еда, гигиена, сон, игра, любовь), ТЗ
аниматора — один вход `mood` с шестью кодами. Как одно превращается в другое,
не написано нигде, поэтому правило вынесено в отдельный класс `BearMoodPolicy`
и помечено как допущение:

1. Из трёх показателей со своим idle (еда → `hungry`, сон → `sleepy`,
   гигиена → `dirty`) берётся самый низкий; если он ниже 30 — играет он.
2. `dirty` пропускается на стадиях 1–2: клипа `idle_dirty` для них нет.
3. Иначе по среднему: ниже 40 — `sad`, от 80 — `happy`, между — `normal`.

Пороги и приоритеты меняются одним конструктором. Правило **подтверждено**
Русланом 10.08.2026.

## Характер, инициатива, реплики, рождение

| Что | Где | Из чего |
| --- | --- | --- |
| Формирование характера из действий | `BearTraitTracker` | КП 7.3 |
| Стартовые склонности по зодиаку | `BearZodiacInfluence` | КП 7.2 |
| Пузырь инициативы | `BearInitiativePolicy` | КП 3.4, 7.4 |
| 30 реплик на ru/en/zh | `BearPhrases` | КП 13.3, 13.4 |
| Сцена рождения с субтитрами и «Пропустить» | `BirthSceneView` | КП 2.1, ТЗ 7.8 |

### Как считается характер

Механики в КП нет — только требование «из совокупности действий за период, а
не из одного действия». Реализовано так:

1. Действия копятся в скользящем окне (3 дня), старое забывается.
2. Судить можно не раньше суток наблюдения.
3. Четыре черты набираются весами действий: играем — активный, учимся —
   любознательный, гладим — ласковый, укладываем — спокойный.
4. «Самостоятельный» и «замкнутый» считаются не весами, а вовлечённостью: это
   черты про отсутствие взаимодействия, и гейт по числу действий к ним не
   применяется — малое число действий и есть их признак.
5. Зодиак добавляет склонность, но не перебивает действия.

Всё это — допущение, вынесенное в один класс.

## Запуск

Платформенные папки в репозитории не хранятся:

```bash
flutter create .          # создаст android/ ios/ и т.д.
flutter pub get
flutter run
```

Требования: Flutter >= 3.32, Dart SDK >= 3.8 (этого требует `rive` 0.14.x).
Нативные библиотеки `rive_native` скачиваются автоматически. Если сборка
падает:

```bash
flutter clean && flutter pub get
dart run rive_native:setup --verbose --clean --platform <macos|ios|android|...>
```

## Тесты

```bash
flutter test
```

95 тестов: показатели и затухание, свёртка в `mood`, контроллер, соответствие
значений входов разделу 8.1, формирование характера, инициатива, комплектность
реплик и субтитров. Тестов рига нет — без реального `.riv` тестировать нечего.

## Как подключить готовый риг

1. Положить `bear_main.riv` в `assets/rive/`.
2. Запустить — плейсхолдер сменится мишкой. Дев-панель покажет, отзывается ли
   риг на каждый вход.
3. Если какого-то входа в машине нет, приложение не упадёт: в дев-лог уйдёт
   сообщение, какого именно. Сверить с `lib/bear/bear_rig_spec.dart`.

## Использование из приложения

```dart
final bear = BearController();

BearView(controller: bear);           // на экране

bear.feedBear();                      // trg_eat + еда вверх
bear.petBear();                       // trg_pet + любовь вверх
bear.washBear();
bear.playWithBear();
bear.putToSleep();
bear.growUp();                        // trg_stage_up, затем stage++
bear.showHappy();                     // после верного ответа в обучении
bear.setOutfit(const BearOutfit(outfitId: 3, headwearId: 1));
bear.restoreState(savedState);        // загрузка прогресса с сервера

// действия из других модулей — влияют на характер (КП 7.3)
bear.recordAction(BearAction.learn);

// пузырь инициативы (КП 3.4)
final initiative = bear.initiative;   // null, если поводов нет
final pause = bear.initiativeCooldown;

// реплика под текущее состояние (КП 13.3)
final phrase = BearPhrases.random(
  BearPhrases.contextForMood(bear.state.mood),
);
phrase?.text(BearLanguage.ru);

// сцена рождения (КП 2.1)
BirthSceneView(language: BearLanguage.ru, onFinished: goToBirthCard);
```

## Открытые вопросы

- **Таблица зодиака** (КП 7.2) — «по таблице Заказчика». Таблицы нет, механика
  готова и по умолчанию не влияет ни на что. Нужна таблица «знак → склонности».
- **Стиль реплик** (КП 13.3) — «пишет Исполнитель, стиль согласуется с
  Заказчиком». 30 реплик написаны как черновик, ждут согласования. После
  утверждения переезжают в панель управления (КП 15.5), здесь останутся как
  офлайн-фолбэк (КП 1.1).
- **Тайминги сцены рождения** — сцена не собрана, субтитры разложены на 26
  секунд по шести тактам из ТЗ 7.8. Свести с реальной анимацией, когда придёт
  файл.
- **Чем показывать сцену рождения** — она сдаётся и как `.riv`, и как mp4.
  Сделан вариант на Rive; переезд на видео затронет только `birth_scene_view.dart`.
- **Скорости затухания, безопасный предел, пороги характера и инициативы** —
  заглушки. По КП 15.4 всё это настраивается с панели управления, то есть
  должно приезжать с сервера.
- **Серверное время** (КП 1.5) — локальный таймер годится только для открытой
  сессии; отыгрыш «сколько прошло, пока приложение было закрыто» обязан
  считаться на сервере.
- **Момент взросления** — по КП 5.7 зависит от общего ухода и настраивается с
  сервера. `growUp()` только исполняет, решения не принимает.

## Принятые решения

| Дата | Вопрос | Решение |
| --- | --- | --- |
| 10.08.2026 | Свёртка показателей в `mood` | «Самая острая нужда» — как реализовано |
| 10.08.2026 | Backend-стек (КП 1.4, 1.5, 15) | **Supabase** |
